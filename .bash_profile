export TERM=screen-256color

export GTEST_COLOR=yes

export LESS="-R"

alias xterm="xterm -ls &"
alias vim="vim -p"
alias bim="vim"

alias ffind="find . -iname"

alias tmux="tmux -u -2"

[-f ~/.fzf.bash ] && source ~/.fzf.bash
export FZF_DEFAULT_OPTS='--hight 40% --layout=reverse --border'

# Use Z
# source ~/.z_dir/z.sh
